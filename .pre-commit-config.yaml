repos:
  - repo: local
    hooks:
      - id: snapper
        name: Format prose with Snapper
        entry: pixi run snapper --format markdown --in-place
        language: system
        files: \.(md|markdown|qmd)$
